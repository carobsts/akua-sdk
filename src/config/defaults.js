export const ENVIRONMENTS = {
  production: "https://api.akua.la",
  sandbox: "https://sandbox.akua.la",
};

export const DEFAULT_TIMEOUT = 10000;

export const DEFAULT_HEADERS = {
  "Content-Type": "application/json",
  Accept: "application/json",
};
